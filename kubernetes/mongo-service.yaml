apiVersion: v1
kind: Service
metadata:
  name: mongo-service
spec:
  clusterIP: None  # Headless service for stable network identity
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
